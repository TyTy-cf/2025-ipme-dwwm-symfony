{% if title is defined %}
    <h2 class="mt-5">{{ title }}</h2>
{% endif %}
<div class="row" data-test="best-offer">
    {% for game in games %}
        {% include 'front/partials/_game_card.html.twig' with {
            'game': game
        } %}
    {% endfor %}
</div>
